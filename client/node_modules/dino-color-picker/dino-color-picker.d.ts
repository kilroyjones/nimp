export declare type Color = [number, number, number, number];
export declare class DinoColorPicker extends HTMLElement {
  constructor();
  get rgb(): Color;
  get hsl(): Color;
  get value(): string; // Hex value
  set value(value: string); // Accepts, hex, rgb, rgba, hsl, hsla
}