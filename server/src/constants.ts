export const CELL_WIDTH = 64;
export const CELL_HEIGHT = 64;
export const REGION_WIDTH = 3200;
export const REGION_HEIGHT = 2048;
export const REGION_WIDTH_CELLS = Math.floor(REGION_WIDTH / CELL_WIDTH);
export const REGION_HEIGHT_CELLS = Math.floor(REGION_HEIGHT / CELL_HEIGHT);
export const UPDATE_DISTANCE = 200;
