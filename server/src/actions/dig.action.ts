import { Post } from "../types";
import { getRandomInt } from "../helpers/random.helper";
// import { CELL_HEIGHT, CELL_HEIGHT_MAX, CELL_WIDTH, CELL_WIDTH_MAX } from "../constants";
import { Location } from "../types";

// const createRegion = async (x: number, y: number) => {
//     return await e
//       .insert(e.Region, {
//         x: region.x,
//         y: region.y,
//         open: e.bool(true),
//         odds: e.int16(10),
//         posts: e.json(posts),
//       })
//       .run(db);
//     console.log(result);
//     socket.to(`${region.x}${region.y}`).emit("update", posts);
//   }
// };

// export const DigActions = {
//   createRegion,
// };
